{
  function increment(x: number): number {
    return x + 1;
  }

  function getFirstCharacter(s: string): string {
    return s?.[0] ?? "";
  }

  function wordScore(word: string): number {
    return word.length;
  }
}
